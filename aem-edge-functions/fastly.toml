# This file describes a Fastly Compute package. To learn more visit:
# https://www.fastly.com/documentation/reference/compute/fastly-toml

authors = []
cloned_from = "https://github.com/fastly/compute-starter-kit-javascript-empty"
description = "Simple AEM Edge Functions Boilerplate"
language = "javascript"
manifest_version = 3
name = "aem-edge-functions-boilerplate"
service_id = ""

[scripts]
  build = "npm run build"
  post_init = "npm install"

[local_server]
  [local_server.backends]
    [local_server.backends.open-meteo]
      url = "https://api.open-meteo.com"
    [local_server.backends.healthwise]
      url = "https://ixbapi.healthwise.net"
    # DEV ONLY: fetch the page shell from local `aem up` so local template/CSS
    # changes show at :7676. Prod/preview use edgeFunctions.yaml (eds-prod/eds-preview).
    [local_server.backends.eds-local]
      url = "http://localhost:3000"

  [local_server.config_stores]
    [local_server.config_stores.config_default]
      format = "inline-toml"
    [local_server.config_stores.config_default.contents]
      LOG_LEVEL = "debug"

  [local_server.secret_stores]
    [[local_server.secret_stores.secret_default]]
      key = "API_TOKEN"
      data = "helloworld"
    # Local Healthwise key — read from a gitignored file so the real secret is
    # never committed. Copy hw_key.local.example to hw_key.local and paste your
    # key. (Production reads HW_KEY from the Cloud Manager secret, not this file.)
    [[local_server.secret_stores.secret_default]]
      key = "HW_KEY"
      file = "hw_key.local"

